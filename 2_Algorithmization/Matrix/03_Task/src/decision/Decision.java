package decision;

public class Decision {
    public static void main(String[] args) {
        Matrix randomMatrix = new Matrix();
        int[][] matrix = randomMatrix.matrix();
        int n = randomMatrix.getN();
        int m = randomMatrix.getM();

        System.out.println(n + " " + m);
        //for(int i = 0; i < )
    }
}
